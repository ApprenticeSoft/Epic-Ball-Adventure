pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Epic-Ball-Adventure"
include "desktop", "android", "html", "core"
